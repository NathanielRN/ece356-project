# UN*X File System - Relational Database
This is our ECE 356 Project Submission made by Eliseo Ruiz Nowell (20667926)
and Gerald Aryeetey (20685073)

# Project Description
This project uses MySQL to implement a UN*X File System using a Relational
Database. An ER Model of the Database is included in the root folder, and the
tables used are all in BCNF and therefore _normalized_.

# System Requirements
Python 3.X+
hon 3.X+
  * PIP
MySQL 8.0+

# Steps to Run Our Project
1. Create an empty MySQL Database `CREATE <database_name>` and run `USE <database_name>`
2. Run `SOURCE fs_schema.sql` within the mysql database 
3. Copy .fs_db_rdbsh template and store it in ./.fs_db_rdbsh
4. Fill in ./.fs_db.rdbsh with database information
5. (Optional) Fill the database with data by running `./fill_fs_rd.py`
6. Run `rdbsh` to enter the fill system